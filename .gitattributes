# Normalize all text to LF in the repo so the Makefile, shell, and Python files
# stay LF on Linux CI regardless of the checkout platform.
* text=auto eol=lf

# Binary assets must never be normalized.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.faiss binary
